{
  "id": "dht22",
  "name": "DHT22 Sensor",
  "description": "Temperature/humidity sensor (single data GPIO)",
  "platforms": ["esp32", "stm32", "rp2040", "avr", "pic16"],
  "provides": ["sensor"],
  "depends": ["gpio", "timer"],
  "conflicts": [],
  "defines": ["BASALT_ENABLE_DHT22"],
  "pins": ["dht22_data"],
  "pin_requirements": {
    "dht22_data": {
      "required": true,
      "description": "DHT22 data pin"
    }
  },
  "configuration_options": [
    {"id": "sample_ms", "name": "Sample Interval (ms)", "type": "number", "default": 2000, "min": 500, "max": 60000}
  ]
}
